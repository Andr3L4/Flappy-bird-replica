
export const GAME_WIDTH = 500;
export const GAME_HEIGHT = 800;

export const PLAYER_WIDTH = 50;
export const PLAYER_HEIGHT = 40;
export const PLAYER_X_POSITION = GAME_WIDTH / 4;

export const GRAVITY = 0.5;
export const JUMP_STRENGTH = -9;

export const PIPE_WIDTH = 80;
export const PIPE_GAP = 220;
export const PIPE_SPEED = 4;
export const PIPE_SPACING_FRAMES = 90; // Generate a pipe every 90 frames
